{
  "name": "dev-workflow",
  "owner": {
    "name": "HCode Technologies"
  },
  "plugins": [
    {
      "name": "dev-workflow",
      "description": "CI, but for ticket work: standup/cleanup/release/blog-from-session/setup session skills + the opt-in ticket-loop agent, in three tiers (v1 local dev, v2 local agent, v3 remote runner), configured per-repo by dev-workflow.yml",
      "source": "./",
      "repository": "singlas/dev-workflow",
      "path": "."
    }
  ]
}
